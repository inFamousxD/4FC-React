import React, { Fragment } from 'react';

export default () => (
    <Fragment>
        <div style={{
            margin: 'auto',
            display: 'block'
        }}>
            <h1>Loading</h1>
        </div>
    </Fragment>
)